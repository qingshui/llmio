# LLMIO 服务配置示例
# 复制为 .env 并填入真实值（.env 已被 .gitignore 忽略，不会入库）
#
# TOKEN: 控制台登录与 API 鉴权凭证，公网访问必填
TOKEN=your_token_here

# 可选覆盖（以下为脚本默认值）
# LLMIO_SERVER_PORT=8070
# GIN_MODE=release
# TZ=Asia/Shanghai
# TRUSTED_PROXIES=127.0.0.1/32   # 生产建议只信任 nginx 所在 IP
